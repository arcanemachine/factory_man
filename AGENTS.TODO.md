# TODO

- Merge other todos into this document
- Clean up documentation:
  - Add "Quick Tour" (name may be improved): Show initial code example with most (all?) features on a single page.
  - Use correct default param syntax: `\\` not `\`
  - Use standardized syntax to indicate a filename: `/path/to/file` above code block, not `# /path/to/file` in code block
